var should = require('should');
var sinon = require('sinon');
var ws = require('../workspace/ws_example');


describe('WebSocket connection model', function(){
	beforeEach(function() {
		// replace the connect method on the connection model
	});

    it('should register onmessage', function() {
        // stuff!
    });
});
